<div class="row" style="padding-top: 100px;">

<% if model.errors.any? %>
  <div>
    <h4>
    	<%= pluralize(model.errors.count, 'error') %> prohibited this position from being save: 
    </h4>
    <hr/>
    <ul>
        <% model.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
    </ul>
  </div>
<% end %>

</div>